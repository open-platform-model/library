# Tasks: one-api-tier

## 1. Additive surface (shippable on its own)

- [ ] 1.1 `opm/kernel/acquire.go`: add `AcquireModuleFromDir(ctx, dir)` (load through the module gate, `module.NewModuleFromValue`, `Source = sourceForDir(absDir)` with `Overlay = sourcetree.OverlayFromDir(src.Root)`); tests in `acquire_test.go`: overlay holds every `.cue` file and nothing else, `HasSource()` true, empty `Pkg` at a module root and a relative `Pkg` for a subdirectory, each gate sentinel propagates, a directory-acquired module passed to `SynthesizeInstance` renders the same objects as the registry-acquired copy (registrytest fixture served both ways).
- [ ] 1.2 `opm/kernel/kernel.go`: `New` builds `schema.OCILoader{Registry: k.registry}` when no `WithSchemaLoader` was given, in either option order; test that `SchemaCache().Get` resolves through a `WithRegistry` mapping pointing at the in-process registry with the process `CUE_REGISTRY` unset, and that an explicit loader still wins.
- [ ] 1.3 `opm/kernel/synth.go`: after the `HasSource` check, refuse `Module.Source.Pkg != ""` with an error naming the root-package requirement; test with a module acquired from a subdirectory.

## 2. Sentinels and the internal loader

- [ ] 2.1 `opm/errors/sentinels.go`: declare `ErrInvalidPackage`, `ErrWrongKind`, `ErrMissingRequiredField`, `ErrMissingModule`, `ErrMissingName`, `ErrMissingNamespace`, `ErrMissingSource`, `ErrSchemaUnavailable` with the doc each carries today; `shape.go` and `synth/instance.go` alias to them so identity is preserved while the packages still exist; `go vet ./...` green.
- [ ] 2.2 Create `opm/internal/loader`: move `shape.go` (gate, specs) and `build.go`'s `buildAndShapeGate` as `LoadDir(ctx, root, pkg string, overlay map[string][]byte, env []string, spec ArtifactSpec)`; move the file-loader tests (`instance_test.go`, `module_test.go`, `platform_test.go`, `validate_test.go`, `validate_internal_test.go`) as tests of `LoadDir` with the three specs; verify the moved tests pass and that the module and platform cases now run through `LoadDir` (the inlined sequences are gone).
- [ ] 2.3 `opm/internal/loader/registry.go`: move `LoadModulePackageWithSource` as `FetchModule(ctx, cueCtx, path, version string, env []string) (cue.Value, *module.Source, error)` with `verifyModuleIdentity`; move `registry/module_test.go`; verify the identity and overlay tests pass unchanged.
- [ ] 2.4 `opm/kernel`: `AcquirePlatformFromDir(ctx, dir)`, `AcquireInstanceFromDir(ctx, dir, values ...Source)`, `AcquireModuleFromDir`, `AcquireModuleFromRegistry` and `attributeValuesError` call `opm/internal/loader` with `cueenv.Override(k.registry, "")`; delete `wrappers.go` (`LoadModulePackage`, `LoadInstancePackage`, `LoadPlatformPackage`, `NewModuleFromValue`, `NewPlatformFromValue`), `AcquireOption`, `WithValues`; extract `mergeSources(sources) (cue.Value, error)` from `loadInstanceWithValues`; migrate every `LoadOptions{}` / `WithValues` / raw-loader call in `opm/kernel` tests onto the acquire verbs; `go test ./opm/kernel/...` green.
- [ ] 2.5 Delete `opm/helper/loader/**`; `go build ./...` green and `grep -r helper/loader opm` finds only the doc and CLAUDE.md lines task 5.1 rewrites.

## 3. Synthesis into the kernel

- [ ] 3.1 Create `opm/internal/synth`: move `instance.go`, `render.go` and their tests; `Input{Module, Name, Namespace, Values cue.Value, Labels, Annotations}`; `Instance(cueCtx, coreVersion string, in Input) (cue.Value, *module.Source, error)` with the `SchemaCache` field, `ErrMissingSchemaCache` and the in-package schema `Get` removed (the kernel supplies `coreVersion`); build through `loader.LoadDir`; verify the moved tests pass with the kernel-supplied version.
- [ ] 3.2 `opm/kernel/synth.go`: declare `InstanceInput{Module, Name, Namespace, Values []Source, Labels, Annotations}`; `SynthesizeInstance` validates required fields against the `opm/errors` sentinels, resolves `coreVersion` from the kernel cache, merges `Values` with `mergeSources`, calls `internal/synth`, runs `validateSources(configSchema, sources, false)` on success so positions name each source's `Origin`, then `processInstance`; migrate the 24 `InstanceInput{}` test sites; add tests: layered sources unify in order, a violating source is attributed to its `Origin`, empty `Values` is not backfilled from `debugValues`, missing inputs wrap the sentinels before any build.
- [ ] 3.3 Delete `opm/helper/synth`; `opm/helper` lists `platformmodule` only; `go build ./...` green.

## 4. Source.Overlay as bytes

- [ ] 4.1 `opm/module/source.go`: `Overlay map[string][]byte` with the doc updated; `opm/internal/sourcetree`: `OverlayFromDir`/`OverlayFromFS` return bytes, `ReadFile` and `WriteTo` use them directly, delete `Bytes` and its tests; `opm/internal/loader.LoadDir` builds the `load.Config.Overlay` with `load.FromBytes`; `opm/internal/synth` clones with `maps.Clone`; `opm/kernel/acquire.go` stores the rendered values file as bytes; `go test ./... ` green and the round-trip test in `sourcetree_test.go` asserts byte equality.
- [ ] 4.2 `opm/module/source.go`: `(*Source).WriteTo(dir string) ([]string, error)` validates every overlay entry (nil receiver, nil `Overlay` and an entry outside `Root` refused with a plain error before anything is written), writes each entry under `dir` at its `Root`-relative path with parents created, and returns the dir-relative paths written, sorted; `opm/internal/sourcetree`: delete `WriteTo`, moving `TestWriteTo_RoundTrip` and its refusal cases to an external test in `opm/module` against the method; `opm/internal/renderstage/stage.go`: `serveDir` calls `src.WriteTo(target)`; verify the round trip (`sourcetree.OverlayFromDir` of the written directory equals the overlay) and `go test ./opm/...` green.

## 5. Boundary gate and docs

- [ ] 5.1 `.golangci.yml`: `depguard` rule denying `github.com/open-platform-model/library/opm/helper` imports from `opm/kernel`, `opm/module`, `opm/platform`, `opm/schema`, `opm/errors`, `opm/core`, `opm/compat`, `opm/internal/**`; verify `task lint` passes and fails when a helper import is temporarily added to `opm/kernel`.
- [ ] 5.2 `opm/helper/doc.go` (platformmodule only, boundary statement), `opm/kernel/doc.go` (surface: five acquire verbs, `SynthesizeInstance` with `InstanceInput`, `ValidateConfigDetailed`, `Render`; example drops `LoadOptions{}`), `CLAUDE.md` (Repository Layout: `opm/internal/loader`, `opm/internal/synth`, helper tree, the `internal/sourcetree` line without `WriteTo` and `Bytes`, `module/` naming `Source.WriteTo`; Kernel API surface and Working Style paragraphs; the `loader/internal/shape` sentence), `README.md`, `docs/getting-started.md`; verify no reference to `loaderfile`, `loaderregistry`, `synth.InstanceInput`, `LoadOptions` or `Load*Package` remains (`grep -rn` over the five files is empty).
- [ ] 5.3 `opm/kernel/kernel_test.go` `TestKernel_PrunedSurface`: add `LoadModulePackage`, `LoadPlatformPackage`, `LoadInstancePackage`, `NewModuleFromValue`, `NewPlatformFromValue`, `WithValues`; verify the test passes.

## 6. Validation gates

- [ ] 6.1 `task fmt`, `task vet`, `task lint`, `task test` green; `go test -race ./opm/kernel/... ./opm/internal/renderstage/...` green; `task cue:test:flow` green against GHCR.
- [ ] 6.2 `go build ./...` and `go vet ./...` in `../cli` and `../opm-operator` against a temporary `replace github.com/open-platform-model/library => ../library`, applying only the consumer edits `proposal.md` § Impact lists, then revert the replace; verify the edit list matches the proposal and no `opm/helper/loader`, `opm/helper/synth`, `CueContext().CompileBytes` or (in `cli/internal/scaffold`) `modconfig.NewRegistry` reference remains in either consumer.
