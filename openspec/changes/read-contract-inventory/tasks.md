# Tasks: read-contract-inventory

Sections per design.md. Section 1 is the spike (design.md § Section 1 is a spike): it lands the pin and proves the suite is green on alpha.9 with no glue change before any reader is written.

## 1. Core pin to v2.0.0-alpha.9 (opm/schema, fixtures)

- [x] 1.1 Move `schema.DefaultSchemaModule` to `opmodel.dev/core@v2.0.0-alpha.9` in `opm/schema/loader.go`, update its doc comment and every doc comment in `opm/kernel` and `opm/schema` citing the pinned identifier, and move the two assertions in `opm/schema/loader_test.go`. Verify: `grep -rn "alpha\.7" opm/schema opm/kernel/*.go | grep -v _test` is empty and `go test ./opm/schema/...` passes.
- [x] 1.2 `task check` green except any fixture-drift assertion naming the served core version, then commit `fix(deps): pin core v2.0.0-alpha.9 as the default schema release`.
- [ ] 1.3 Re-pin the fixtures: `task cue:deps:update` for the four discovered modules (`modules/opm_platform`, `testdata/modules/web_app`, `testdata/parity`, `testdata/parity/opm_platform`; `task cue:tidy` leaves no further diff); `registrytest.DefaultCoreVersion` and its doc; every `testdata/render/**/cue.mod/module.cue` core dep and the alpha.7 mentions in the `testdata/render/*/platform.cue` headers; the version literals in `opm/kernel/render_test.go`, `opm/internal/renderstage/modfile_test.go` (keep the deliberately older `alpha.6` module-side row at line 428 as the skew case) and `stage_test.go`. Verify: `grep -rn "alpha\.7" opm testdata modules --include=*.go --include=*.cue` reports only `closure_test.go`'s own `coreVersion` graph, and `task test` is green with no change under `opm/internal/renderstage/render.cue.tmpl`.
- [ ] 1.4 `task check` green, then commit `test(fixtures): re-pin the CUE fixtures and served registry to core alpha.9`.

## 2. The inventory off a built platform (opm/schema, opm/platform, testdata/render)

- [ ] 2.1 Spike the read: on the `platform` render fixture acquired through `newRenderKernel`, `Package.LookupPath(cue.MakePath(cue.Def("contracts")))` exists and `definedBy`, `requiredBy`, `unfulfilled`, `overSubscribed`, `fulfilled`, `routable` each decode by path (a throwaway test, deleted before the commit); record in design.md § Decisions whether one `Decode` over the struct also works once `defined` is excluded. Verify: the throwaway test passes and the finding is written.
- [ ] 2.2 List every member of `testdata/render/registry`'s `cat` and `cat2` catalogs (both `v0.1.0` and `v0.2.0`) in `#resources` and `#traits`, keyed by `metadata.fqn`; rewrite each catalog header's member paragraph. Verify: `cue vet` on each served catalog directory passes, and `task test:run TEST=TestRender` (the render suite) is unchanged, since the guard is unchanged.
- [ ] 2.3 Add `schema.Contracts` to `opm/schema/paths.go` (with the inventory comment naming its reader) and `ContractInventory` plus `(*Platform).Contracts()` in `opm/platform/contracts.go` per design.md § The Go surface: per-path reads, `defined` not decoded, an error naming the missing field when the platform carries no `#contracts`. Verify: `go vet ./opm/platform/...` passes and the type's doc states both exclusions.
- [ ] 2.4 `opm/platform/contracts_test.go` against the render fixtures: `platform` (the spec's healthy scenario, values pinned from 2.1), `platform_oversubscribed` (`OverSubscribed` names the key, `Routable` false, nil error from acquisition and accessor), a platform over a catalog with empty maps (`Fulfilled` and `Routable` true, empty maps), and `NewPlatformFromValue` over a `#Platform` value lacking `#contracts` (the missing-field error). Verify: `go test ./opm/platform/...` passes and the over-subscribed case asserts no error.
- [ ] 2.5 `task check` green, then commit `feat(platform): read the contract inventory off a built platform`.

## 3. The defining catalog on the unresolved-demand row (opm/errors, opm/internal/renderstage, opm/kernel)

- [ ] 3.1 Add `DefinedBy string` to `oerrors.UnresolvedDemand` and reword `describe()` for the three cases in the spec (defined by a named catalog and unimplemented; implemented at another apiVersion; no enabled catalog defines it). Verify: a table test in `opm/errors/match_test.go` covers all three sentences.
- [ ] 3.2 Fill `definedBy` on the glue's `unresolvedResources` and `unresolvedTraits` rows from `platform.#contracts.definedBy` per design.md § The row (default `""`, guarded lookup). Verify: `render_decode.go` needs no change (the row decodes by tag), and `go test ./opm/kernel/... -run TestRender` still passes on every existing scenario.
- [ ] 3.3 Render tests for the new arm on the listed fixtures: a load-bearing trait the `cat` catalog lists and no transformer requires carries `cat`'s registry key on its row and the message names it; a key no catalog lists carries an empty `DefinedBy` and the "no enabled catalog defines" wording; a platform whose only listing catalog is `enable: false` carries an empty `DefinedBy`. Verify: `go test ./opm/kernel/... -run TestRender` passes with the three new cases and the render-parity suite is unchanged.
- [ ] 3.4 Run the cross-cutting checks: `task cue:test:flow`, and a build of `cli` and `opm-operator` against this tree with a `replace` in a scratch copy of each `go.mod` (not committed) to confirm no consumer compiles against a changed signature. Verify: both build; any cli test asserting the old unresolved-demand sentence is noted in the proposal's downstream section by file and line.
- [ ] 3.5 `task check` green, then commit `feat(kernel): name the defining catalog on an unresolved demand`.
