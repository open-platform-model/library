# Tasks: cue-owned-verdicts

## 1. `Compiled` moves into the kernel (`opm/kernel`, `opm/core`)

- [x] 1.1 `opm/kernel/render.go`: declare `Compiled` beside `RenderResult` with the four fields and the doc `core.Compiled` carries today (`Value`, `Instance`, `Component`, `Transformer`); `RenderResult.Compiled` becomes `[]*Compiled`; `render_decode.go`: `decodeRendered` and `splitOutput` construct `*Compiled`; delete `opm/core`; migrate the three `core.Compiled` references in `render_test.go` and `flow_synth_imported_test.go`; verify `go build ./...` is green and `grep -rn 'opm/core\|core\.Compiled' opm` is empty.
- [x] 1.2 Docs for the move: `CLAUDE.md` (Repository Layout: drop the `core/` line; Kernel API surface bullet; the `*core.Compiled` paragraph; the pipeline diagram's `decodeRendered` line), `README.md` (pipeline diagram and both prose mentions), `CONSTITUTION.md` III (drop the `opm/core/` line), `docs/getting-started.md` (intro sentence, the provenance paragraph, the verb table), `.claude/skills/verify/SKILL.md` (field inspection line); verify `grep -rn 'opm/core\|core\.Compiled' CLAUDE.md README.md CONSTITUTION.md docs .claude` reports only `docs/design/transformer-output-hidden-field-scope-bug.md`, the historical bug record whose own banner states the code it analyses no longer exists (it names `Materialize`, `opm/compile` and `FillPath` the same way) — a record of what was true then is not a stale reference, and renaming one symbol in it would make the account internally inconsistent.

## 2. Glue emits the verdict rows (`opm/internal/renderstage`)

- [x] 2.1 `render.cue.tmpl`: add `#apiVersionLess` (regexp over `v(\d+)(alpha|beta)?(\d+)?`; level, major, minor, then key string) and make `unresolvedResources` / `unresolvedTraits` rows carry `alternatives` (same-base keys of the demand's own bucket universe, `list.Sort` on the comparator) and `disqualified: [{transformer, conflicts: _unify[tfqn].conflicts}]`; also move the single-provider guard's ordering into the build, since the decoder no longer sorts what it decodes: `guard.overSubscribed` becomes `list.Sort` on the key and each row's `catalogs` becomes `list.Sort(..., list.Ascending)`; verify with a `render_test.go` fixture demanding a base implemented at `v1alpha1`, `v1beta1` and `v1` that the decoded alternatives arrive in that order, that a disqualified candidate's row lists its conflicting FQNs, and that the over-subscription test still sees its catalogs sorted.
- [x] 2.2 `render.cue.tmpl`: replace `unmatchedComponents` and `candidates` with `unmatched: [{component, candidates: [{transformer, matched, missingLabels}]}]` sorted by component then transformer; drop `bucketKeys`, `missing` and `resolved` from `diagnostics` (keep `match.resolved` for `gate`, rebased onto `unmatched`), and with `missing` gone delete the per-component `missingResources` / `missingTraits` lists it was the only reader of (the `_pred` rung keeps its own same-named fields); verify the existing predicate-refusal test reads the missing label off `Diagnostics.Unmatched[0].Candidates[0]` and that `grep -n 'bucketKeys\|unmatchedComponents' render.cue.tmpl` is empty.

## 3. Decoder reads rows flat (`opm/kernel`)

- [x] 3.1 `render_decode.go`: `glueDiagnostics` mirrors the new export exactly; delete `byCandidate`, `matchMatrix`, `pairsOf`'s sibling helpers that only served the join, and the regroup in `gateErrors`; `gateErrors` builds the three aggregates directly from the decoded slices; assert `dv.Validate(cue.Concrete(true))` before decoding and raise `unstatedPosture` from there (dropping `resolved` from the export removes the scalar that used to make an unstated posture fail the decode — an incomplete comprehension decodes as an empty list, which would read as "no verdict" rather than "verdict unknown"); delete `opm/internal/renderstage/alternatives.go` and `TestAlternatives`; verify `go build ./...` shows no `opm/compat` import under `opm/internal` (`go list -deps ./opm/internal/renderstage | grep compat` is empty) and `go test ./opm/kernel/... ./opm/internal/renderstage/...` is green.
- [x] 3.2 `render_test.go`: add `assertGateAgrees(t, built, refused bool)` reading `gate` off the built value (expose the built value to tests through the existing internal test seam or a `renderForTest` helper) and call it from every refusal and success test in the file; verify each test passes and that flipping the helper's expectation fails it.

## 4. Rows and aggregates (`opm/errors`)

- [x] 4.1 Declare the rows: `UnifyRefusal{Component, Transformer, Conflicts}` replaces `UnifyError`; `CandidateVerdict{Transformer, Matched, MissingLabels}` replaces `MatchResult`; `UnmatchedComponent{Component, Candidates}`; `OverSubscribedContract{Key, Catalogs}` replaces `OverSubscribedContractError`; `UnresolvedDemand.Disqualified []UnifyRefusal` and no `Error` method on any row; `TransformError{Component, Transformer, Cause}`; update `RenderDiagnostics` field types and `decodeRendered`'s `TransformError` sites; verify `go vet ./...` is green and `grep -rn 'UnifyError\|MatchResult\|ComponentName\|TransformerFQN' opm` is empty.
- [x] 4.2 Declare the aggregates: `*UnmatchedComponentsError{Components []UnmatchedComponent}` with `Error()` listing each component and its unmatched candidates and no `Unwrap`; `*OverSubscribedContractsError{Contracts}` joined into the gate once; `*UnresolvedDemandsError` without `Unwrap`; rewrite `match_test.go`, `unmatched_test.go`, `oversubscribed_test.go` as message-shape and `errors.AsType` tests over the new types; verify every test in `opm/errors` passes and that `errors.AsType[*oerrors.TransformError]` on an unmatched-components refusal returns false.

## 5. Warnings leave the result (`opm/kernel`)

- [x] 5.1 `render.go`, `render_decode.go`: delete `RenderResult.Warnings`, the skew sentence and `unhandledTraitWarnings`; reword the `SkewWarn` and `RenderDiagnostics` docs to name `ResolvedVersions[].Newer` and `UnhandledTraits` as the advisory sources; update the `Warnings` assertions in `render_test.go` (optional-trait, skew-warn, determinism and empty-warnings tests), `integration_test.go`, `flow_integration_test.go` and `flow_synth_catalog_import_test.go` to read the rows; verify `go test ./opm/kernel/...` is green and `grep -rn 'Warnings' opm` is empty.

## 6. Docs

- [x] 6.1 `CLAUDE.md` § Render contract (verdicts list, "warned by default" sentence), `opm/kernel/doc.go` (result shape, example loop), `opm/errors` package doc (rows versus causes), `docs/getting-started.md:213-235` (the `result.Warnings` loop becomes a loop over the two diagnostics fields); verify `grep -rn 'result.Warnings\|RenderResult.Warnings\|UnifyError\|MatchResult' CLAUDE.md README.md docs opm/kernel/doc.go` is empty.

## 7. Validation gates

- [x] 7.1 `task fmt`, `task vet`, `task lint`, `task test` green; `go test -race ./opm/kernel/... ./opm/internal/renderstage/...` green; `task cue:test:flow` green against GHCR; verify `openspec validate --change cue-owned-verdicts` passes.
- [x] 7.2 Confirm the consumer breakage this change causes, without migrating either consumer: in `../cli` and `../opm-operator`, add a temporary `replace github.com/open-platform-model/library => ../library`, run `go build ./...`, record the failing sites, then revert the replace leaving both trees clean. Verify the recorded sites match `proposal.md` § Impact (the operator's `pkg/core/compiled_adapter.go` reads `*kernel.Compiled`; the cli imports `opm/core` nowhere), and note in the run that both consumers additionally fail on `opm/helper/loader/file` and `opm/helper/synth` — that is `one-api-tier`'s cost, not this change's. The edits themselves are owned by the sibling changes `migrate-kernel-api-and-verdicts` in `cli` and in `opm-operator`, which land against the alpha carrying both library changes; nothing in this change edits another repo.
