# Tasks: read-comparable-predicates

Sections per design.md. Section 1 is the spike (design.md § Section 1 is a spike): it lands the pin and proves the suite is green on core alpha.10 and catalog 4.4.0 with no glue change before any reader is written.

## 1. Core pin to v2.0.0-alpha.10 (opm/schema, fixtures)

- [x] 1.1 Confirm the precondition: core release PR 69 is merged and `opmodel.dev/core@v2.0.0-alpha.10` resolves from GHCR (a scratch module under the scratchpad running `cue mod get opmodel.dev/core@v2.0.0-alpha.10` with the workspace `CUE_REGISTRY`). Verify: the fetch succeeds; if it does not, stop the section here with nothing edited.
- [x] 1.2 Move `schema.DefaultSchemaModule` to `opmodel.dev/core@v2.0.0-alpha.10` in `opm/schema/loader.go`, update its doc comment and every doc comment in `opm/` citing the pinned release (`opm/platform/contracts.go`, `opm/catalog/requires.go`, `opm/internal/renderstage/modfile.go`), and move the two assertions in `opm/schema/loader_test.go`. Verify: `grep -rn "alpha\.9" opm --include=*.go | grep -v _test.go` is empty and `go test ./opm/schema/...` passes.
- [x] 1.3 `task check` green except the fixture-drift canary (`registrytest_test.go` asserting `DefaultCoreVersion` equals the default release), then commit `fix(deps): pin core v2.0.0-alpha.10 as the default schema release`.
- [ ] 1.4 Re-pin the fixtures: `task cue:deps:update` for the four discovered modules (`modules/opm_platform`, `testdata/modules/web_app`, `testdata/parity`, `testdata/parity/opm_platform`; `task cue:tidy` leaves no further diff; catalogs/opm moves to 4.4.0 with it), `registrytest.DefaultCoreVersion` and its doc, `testdata/cue.mod`, every `testdata/render/**/cue.mod/module.cue` core dep including `scenarios`, the alpha.9 mentions in the five `testdata/render/platform*/platform.cue` headers and `modules/opm_platform/platform.cue`, the version literals in `opm/kernel/render_test.go`, `opm/internal/renderstage/modfile_test.go` (keep the deliberately older skew rows), `stage_test.go`, `opm/helper/platformmodule/generate_test.go` and `opm/catalog/requires_test.go`, and the seven `@4.3.1` case literals in `opm/kernel/parity_harness_test.go` to `@4.4.0`. Verify: `grep -rn "alpha\.9" opm testdata modules --include=*.go --include=*.cue` reports only the deliberately older rows, `task test` is green with no change under `opm/internal/renderstage/render.cue.tmpl`, and `OPM_FLOW_TEST_FORCE=1 go test ./opm/kernel -run 'TestParity|TestFlow'` is green (a parity divergence on 4.4.0 is recorded in design.md § Risks before continuing).
- [ ] 1.5 `task check` green, then commit `test(fixtures): re-pin the CUE fixtures and served registry to core alpha.10 and catalog 4.4.0`.

## 2. The comparable-predicate report off a built platform (opm/platform)

- [ ] 2.1 Spike the read: on `platform_oversubscribed` and `platform` acquired through `contractsKernel`, `#contracts.comparable` and `#contracts.discriminated` exist and decode by path (a throwaway test, deleted before the commit); record the rows in design.md § Fixture values pinned (expected: two rows over `container@v1`, `mirror-transformer@0.2.0` broader against `deployment-transformer@0.1.0` and against `service-transformer@0.1.0`, `discriminated` false; `platform` empty and true). Verify: the throwaway test passes and the finding is written, corrected if the measured rows differ.
- [ ] 2.2 In `opm/platform/contracts.go`, add `ComparablePredicates` and the `Comparable` and `Discriminated` fields per design.md § The Go surface, extend the per-path read table with the two fields and the first-release column appended to the missing-field error, and update the type and accessor doc comments (eight data fields; the report-never-defaulted rule). Verify: `go vet ./opm/platform/...` passes and the doc states that a missing report field refuses, naming the release.
- [ ] 2.3 `opm/platform/contracts_test.go`: the healthy `platform` case asserts empty `Comparable` and `Discriminated` true; `platform_oversubscribed` asserts the rows pinned in 2.1 with `ElementsMatch`, `Discriminated` false and `Routable` false unchanged; the cat2-only and `platform_disabled` cases assert empty and true; a `CompileString` value carrying `#contracts` with the six fields and no `comparable` asserts a nil inventory and an error naming `comparable` and `2.0.0-alpha.10`. Verify: `go test ./opm/platform/...` passes.
- [ ] 2.4 `task check` green, then commit `feat(platform): read the comparable-predicate report off a built platform`.

## 3. The shipped catalog stays discriminated (opm/kernel) and cross-cutting checks

- [ ] 3.1 Add `TestParity_ShippedCatalogDiscriminated` to `opm/kernel/parity_harness_test.go` per design.md § The parity pin is its own test function: gated like the shipped group, acquires `testdata/parity/opm_platform`, asserts `Contracts()` reads `Discriminated` true and an empty `Comparable`, and on failure prints every row as `broader -> narrower over [contracts]`. Verify: `OPM_FLOW_TEST_FORCE=1 go test ./opm/kernel -run TestParity_ShippedCatalogDiscriminated` passes against catalog 4.4.0, and a temporarily inverted expectation (not committed) shows the row-naming message.
- [ ] 3.2 Cross-cutting checks: `task cue:test:flow`, and a build plus `go vet ./...` of `cli` and `opm-operator` against this tree with a `replace` in a scratch copy of each `go.mod` (not committed). Verify: both build; any consumer test asserting the six-field inventory or the alpha.9 literal is noted in the proposal's downstream section by file and line.
- [ ] 3.3 `task check` green, then commit `test(kernel): pin the shipped catalog discriminated in the parity harness`.
