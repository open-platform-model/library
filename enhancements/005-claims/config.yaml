id: "005"
slug: claims
title: "`#Claim` and Module Extension Surface"

status: draft
semver: minor

created: 2026-04-28
updated: 2026-05-17

# 005 and 006 are competing approaches to the same problem (ecosystem-supplied
# needs flowing typed values back to a module). They are NOT cross-linked via
# `related`; the relationship is captured via `competes_with` below, which
# `task enhancements:graph` renders as a dashed-red edge.

authors:
  - OPM Contributors

implementation:
  status: partial
  notes: >-
    Only the #defines slot ships in v1alpha2 (resources, traits, transformers; claims deferred).
    Five of the eight #Module slots present; #claims, #lifecycles, #workflows not yet wired.

related:
  - "003"
  - "004"

competes_with:
  - "006"

supersedes: []
superseded_by: null
