id: "006"
slug: platform-capabilities
title: Platform Capabilities

status: draft
semver: minor

created: 2026-05-14
updated: 2026-05-17

authors:
  - OPM Contributors

implementation:
  status: in-progress
  notes: >-
    Experiments 01 (matcher mechanics) and 02 (read portability + FillPath) concluded
    (2026-05-15); D5 revised per F1 (#ContextBuilder must be invoked at top level, not
    inside #ModuleRelease).

# 005 and 006 are competing approaches to the same problem (ecosystem-supplied
# needs flowing typed values back to a module). They are NOT cross-linked via
# `related`; the relationship is captured via `competes_with` below, which
# `task enhancements:graph` renders as a dashed-red edge.
related:
  - "003"
  - "004"

competes_with:
  - "005"

supersedes: []
superseded_by: null
