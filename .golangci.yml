# golangci-lint v2 configuration.
# Kept intentionally small: the standard linter set plus formatting checks.
# This is a library, not the operator — no custom logcheck plugin.
version: "2"

linters:
  default: standard

formatters:
  enable:
    - gofmt
    - goimports
